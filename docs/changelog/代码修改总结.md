# 中药材烘干问题 - 代码修改总结

## 修改日期
2026年9月11日

## 问题背景
之前的代码使用了**全局数据**和**全域参数**进行拟合，但题目要求只分析**预热阶段**的数据。

## 修改内容

### 1. 核心修改：数据范围限制
**修改文件**：
- `D:\mcm-kitpip-cache\analyze_moisture_vs_temperature.py`
- `D:\mcm-kitpip-cache\data_analysis_with_your_model.py`

**修改内容**：
- 在 `load_data()` 函数中添加 `use_preheat_only=True` 参数
- **只加载前130个数据点**（预热阶段）
- 数据范围：t=0~7740s, T=28.00~49.94°C

```python
def load_data(use_preheat_only=True):
    """加载附件1数据
    
    Args:
        use_preheat_only: 如果为True，只使用前130个数据点（预热阶段）
    """
    # ... 加载数据 ...
    
    # 只使用预热阶段数据（前130个点）
    if use_preheat_only:
        t = t[:130]
        T = T[:130]
        C = C[:130]
    
    return t, T, C
```

### 2. 参数更新：使用预热阶段子集参数

**旧参数（全域）**：
```python
a = 0.01332
b = 0.0009421
c = 0.0731
```

**新参数（预热阶段130点，四舍五入版）**：
```python
a = 0.0118
b = 0.001318  # 1.318×10⁻³
c = 0.067
```

**拟合模型**：
```
w = 0.0118 + 0.001318 × e^(0.067×T)
```

### 3. 文档更新

**修改文件**：
- `D:\mcm-kitpip-cache\README.md`

**更新内容**：
- 更新拟合方程为预热阶段参数
- 添加数据范围说明
- 更新拟合精度指标

## 修改效果对比

| 指标 | 全域参数 | 预热子集参数（新） | 改善 |
|------|---------|------------------|------|
| **数据点数** | 全部 | 前130点 | ✅ 符合题目要求 |
| **R²** | 0.9971 | 0.9980 | ✅ +0.09% |
| **RMSE** | 0.000477 | 0.000402 | ✅ -15.7% |
| **MAE** | 0.000405 | 0.000336 | ✅ -17.0% |
| **MAPE** | - | 0.89% | ✅ 精度极高 |
| **残差均值** | -1.69e-4 | -4.67e-5 | ✅ 更接近0 |

## 验证结果

运行 `python analyze_moisture_vs_temperature.py` 后：

✅ **数据加载**：130 个数据点
✅ **温度范围**：28.00 - 49.94 °C
✅ **水分范围**：0.019630 - 0.049540 kg/kg
✅ **R² = 0.99798**（拟合精度极高）
✅ **RMSE = 0.000402 kg/kg**（比全域参数低15.8%）
✅ **残差服从正态分布**（Shapiro-Wilk检验 p=0.124）
✅ **相关系数 r = 0.985**（极强正相关）

## 残差分析

**残差统计**：
- 均值: -4.67e-05（接近0）
- 标准差: 0.000399
- 最大值: 0.000997
- 最小值: -0.000897

**诊断结果**：
- ✅ 残差服从正态分布
- ⚠️ 存在正自相关（DW=0.857），这是因为指数形式无法完全匹配数据真实曲率

## 生成的文件

所有结果保存在：`D:\mcm-kitpip-cache\outputs\moisture_vs_temperature\`

1. **拟合模型对比表.xlsx** - 多个模型的对比
2. **水分vs温度_拟合对比图.png** - 7个模型的拟合曲线和残差图
3. **你们模型的残差分析.png** - 4合1残差分析图
4. **你们的模型_vs_最佳模型.png** - 与三次多项式的对比
5. **分析总结.txt** - 文本格式的分析报告

## 结论

✅ **代码已成功修改为只使用预热阶段（前130点）数据**
✅ **参数已更新为预热子集拟合参数**
✅ **拟合精度显著提高（RMSE降低15.8%）**
✅ **符合题目要求：只分析预热阶段**

## 注意事项

⚠️ **适用范围限制**：
- 该参数仅适用于 T∈[28, 49.94]°C 范围
- 不要外推到这个温度区间之外
- 残差存在系统性偏差（温度分段振荡）

⚠️ **模型限制**：
- 残差非白噪声（存在自相关）
- 90.6%的残差方差来自模型系统性偏差
- 如需更高精度，可考虑其他模型（如三次多项式R²=0.9986，但参数不稳定）

---

**修改人**：Claude (Kiro)  
**日期**：2026年9月11日
